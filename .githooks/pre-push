#!/usr/bin/env sh
set -eu

if ! command -v bun >/dev/null 2>&1; then
	printf "[pre-push] Bun not found in PATH; skipping local checks (CI will enforce).\n"
	exit 0
fi

printf "[pre-push] Running Bun test suite...\n"
bun test || { printf "Bun tests failed.\n"; exit 1; }

printf "[pre-push] OK.\n"
